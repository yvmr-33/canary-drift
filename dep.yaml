apiVersion: argoproj.io/v1alpha1
kind: Rollout
metadata:
  name: myservice
spec:
  replicas: 10 # Total replicas (was 15 + 1)
  selector:
    matchLabels:
      app: myservice
  template:
    metadata:
      labels:
        app: myservice
    spec:
      containers:
      - name: myservice
        image: docker.io/yvmr/v1:latest 
        ports:
        - containerPort: 3000
        env:
        - name: VERSION
          value: "v1"
  # Define the Canary strategy
  strategy:
    canary:
      
      # References the services created above
      stableService: myservice-stable
      canaryService: myservice-canary
      # Configures Istio traffic routing
      trafficRouting:
        istio:
          virtualService:
            name: myservice-vs
            routes:
            - http
      analysis:
          templates:
          - templateName: error-rate-check 
          args:
          - name: stable-service
            value: myservice-stable.default.svc.cluster.local
          - name: canary-service
            value: myservice-canary.default.svc.cluster.local
      steps:
      - setWeight: 10 
      - pause: {}
      - setWeight: 50
      - pause: {} 
      - setWeight: 90 
      - pause: {}
      - setWeight: 100 
